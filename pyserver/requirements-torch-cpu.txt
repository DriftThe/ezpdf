# CPU variant; Rust passes index-url per platform (pyenv.rs): Linux needs the /cpu index because
# PyPI's Linux torch bundles CUDA (~5GB), while Windows PyPI torch is already CPU-only.
torch==2.13.0
torchvision==0.28.0
