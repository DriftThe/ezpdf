# pyserver base deps (no torch; GPU detection picks the cuda/cpu variant, see requirements-torch-*.txt)
# Versions match the frozen Wise-Paddle known-good set (Python 3.12)
fastapi==0.139.2
uvicorn==0.51.0
transformers==5.14.1
pillow==12.3.0
numpy==2.3.5
safetensors==0.8.0
tiktoken==0.13.0  # required to load the PaddleOCR-VL-1.6 tokenizer
sentencepiece==0.2.2  # tokenizer.model is SentencePiece protobuf
protobuf==7.35.1  # required by SentencePieceExtractor; without it the file is misread as tiktoken
opencv-contrib-python==4.10.0.84  # required by PP-DocLayoutV3 post_process_object_detection
