Code review 已通过，无需改代码。批准后执行：

1. 验证编译：在 src-tauri/ 跑 `cargo check`；根目录跑 `pnpm build`（vue-tsc 类型检查）。如有编译错误则修复后重验。
2. 提交信息已定稿（用户自行执行 git commit，不代跑）：

```
feat(ocr): add cuda availability check

probe nvidia-smi directly from rust; spawn failure means no
nvidia gpu, which is the norm, so it maps to false instead of
panicking. non-windows stub keeps generate_handler compiling.
```

涉及文件：src-tauri/src/lib.rs、src/stores/parse.ts、src/components/settings/sections/OcrSection.vue